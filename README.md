# Gallery-Manager
Gallery Manager is a browser-based image management system built with HTML, CSS, and JavaScript. Users can create accounts, organize images into folders, upload photos, view them in fullscreen, and delete them. It uses IndexedDB for storing images and LocalStorage for user and folder data.
